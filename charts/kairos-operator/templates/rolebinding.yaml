apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kairos-operator.fullname" . }}-leader-election-rolebinding
  namespace: {{ include "kairos-operator.namespace" . }}
  labels:
    {{- include "kairos-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kairos-operator.fullname" . }}-leader-election-role
subjects:
- kind: ServiceAccount
  name: {{ include "kairos-operator.serviceAccountName" . }}
  namespace: {{ include "kairos-operator.namespace" . }}
