apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kairos-operator.fullname" . }}-rolebinding
  labels:
    {{- include "kairos-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kairos-operator.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "kairos-operator.serviceAccountName" . }}
  namespace: {{ include "kairos-operator.namespace" . }}
