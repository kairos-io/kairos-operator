{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "kairos-operator.namespace" . }}
  labels:
    {{- include "kairos-operator.labels" . | nindent 4 }}
{{- end }}
